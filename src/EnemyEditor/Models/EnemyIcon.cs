namespace EnemyEditor.Models;

// Иконка хранит имя файла и полный путь.
public class EnemyIcon
{
    public string Name { get; set; } = string.Empty;
    public string ImagePath { get; set; } = string.Empty;
}
